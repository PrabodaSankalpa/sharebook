import { createTheme } from "@mui/material";


const theme = createTheme({
  palette: {
    primary: {
      main: '#E57C23', // Customize primary color
    },
    secondary: {
      main: '#025464', // Customize secondary color
    },
  }
});

export default theme;
